import {runContextClosureWave3GoldenWorld} from '../tests/context-closure-wave3-harness.js';const r=runContextClosureWave3GoldenWorld();for(const [name,pass] of Object.entries(r.metrics))console.log(`${pass?'PASS':'FAIL'} ${name}`);console.log(`Context Closure Wave 3 acceptance: ${Object.values(r.metrics).filter(Boolean).length}/${Object.values(r.metrics).length}`);if(!r.pass)process.exitCode=1;