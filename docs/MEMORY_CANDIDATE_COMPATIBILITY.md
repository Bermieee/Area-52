# Memory Candidate Compatibility — FT003 Side Readiness

Memory is not implemented on this branch. Wave 4 provides contract fixtures for:

- SceneEpisode;
- episodic memory;
- reflection;
- causal hypothesis;
- historical state;
- current state;
- unresolved competing evidence.

Supplied `OBSERVED`, `INFERRED`, `SETTLED` and `UNRESOLVED` distinctions survive CandidateBus and Precision. Precision may rank these candidates; it cannot promote their authority.

Tested Sidecar path:

`Memory candidate fixture -> CandidateBus -> retrieval quality -> Precision -> Truth-compatible evidence set`

Status: **COPROCESSOR PRECISION SIDE READY FOR FT003**.

This is not FT003 PASS. Real Memory/Temporal State implementation and integrated `main` acceptance remain external.
