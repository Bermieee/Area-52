import { KnowledgeStatus, createCompiledContextPacket } from './contracts.js';
import { stableHash } from './browser-runtime-utils.js';
import {normalizeActiveThreads,buildSemanticPriority,computeSemanticSizing,defaultRepresentationEligibility,semanticPriorityForFact} from './context-compiler-contracts.js';
const sectionFor=(classification)=>classification===KnowledgeStatus.CURRENT?'current':[KnowledgeStatus.HISTORICAL,KnowledgeStatus.SUPERSEDED].includes(classification)?'historical':'unresolved';
const factKey=(claim,section)=>`${section}|${claim.subjectId}|${claim.predicate}|${JSON.stringify(claim.value)}`;
const hash=(value)=>stableHash(String(value),{length:16,alreadyString:true});
export class ContextCompiler {
  constructor({graph,maxFactsPerSection=12,isCurrentRevision=()=>true}){this.graph=graph;this.maxFactsPerSection=maxFactsPerSection;this.isCurrentRevision=isCurrentRevision;}
  compile(input){return this.compileDetailed(input).packet;}
  compileDetailed({query,intent,truthResults,activeThreads=[],knowledgeEvidence=[]}){
    const threadState=normalizeActiveThreads(activeThreads,{isCurrentRevision:this.isCurrentRevision}),threads=threadState.admitted;
    const evidenceByClaim=new Map();
    for(const evidence of knowledgeEvidence??[])for(const claimId of evidence?.claimIds??[]){const rows=evidenceByClaim.get(claimId)??[];rows.push(evidence);evidenceByClaim.set(claimId,rows);}
    const buckets={current:new Map(),historical:new Map(),unresolved:new Map()},provenanceIndex={},knowledgeTraceIndex={},dependencies=new Set();
    for(const result of truthResults){if(!result.usableForIntent)continue;for(const claimId of result.claimIds){const claim=this.graph.getClaim(claimId);if(!claim)continue;const section=sectionFor(result.classification),key=factKey(claim,section);let fact=buckets[section].get(key);if(!fact){fact={e:claim.subjectId,p:claim.predicate,v:claim.value,a:claim.authorityClass,cf:claim.confidence,id:claim.id,_supportIds:[],_from:claim.temporal?.validFrom??null,_to:claim.temporal?.validUntil??null,_status:result.classification,_knowledge:[]};buckets[section].set(key,fact);}fact._supportIds.push(claim.id);fact.cf=Math.max(fact.cf,claim.confidence);if(fact._from===null||Number(claim.temporal?.validFrom??0)<Number(fact._from))fact._from=claim.temporal?.validFrom??fact._from;const refs=claim.provenance?.sourceRevisionIds??[];const knowledge=evidenceByClaim.get(claim.id)??[];fact._knowledge.push(...knowledge);const knowledgeRefs=knowledge.flatMap(x=>x?.sourceRevisionRefs??[]);const existing=provenanceIndex[fact.id]??[];provenanceIndex[fact.id]=[...new Set([...existing,...refs,...knowledgeRefs])].sort();for(const ref of [...refs,...knowledgeRefs])dependencies.add(ref);}}
    for(const thread of threads){provenanceIndex[thread.id]=[...thread.sourceRevisionIds];for(const ref of thread.sourceRevisionIds)dependencies.add(ref);}
    const finalize=(map,section)=>[...map.values()].map(f=>{const out={e:f.e,p:f.p,v:f.v,a:f.a,cf:f.cf,id:f.id};const support=[...new Set(f._supportIds)].sort();if(support.length>1)out.supportIds=support;if(section!=='current')out.t=[f._from,f._to,f._status];const knowledge=[...new Map((f._knowledge??[]).map(x=>[x.evidenceId,x])).values()];if(knowledge.length){out.q=knowledge.map(x=>({evidenceId:x.evidenceId,sourceClass:x.sourceClass,authorityClass:x.authorityClass,temporalStatus:x.temporalStatus,currentApplicability:x.currentApplicability??null,hardRule:Boolean(x.hardRule)}));knowledgeTraceIndex[f.id]=knowledge.map(x=>({evidenceId:x.evidenceId,artifactRef:structuredClone(x.artifactRef),sourceRevisionRefs:[...(x.sourceRevisionRefs??[])],dependencyRevisionRefs:[...(x.dependencyRevisionRefs??[])],provenanceRefs:[...(x.provenanceRefs??[])],retrievalChannels:[...(x.candidateLineage?.nominationChannels??[])],precisionReasons:[...(x.retrievalMetadata?.precision?.reasonCodes??[])],contradictionSetId:x.contradictionSetId??null,hypothesisSetId:x.hypothesisSetId??null}));}return out;}).sort((a,b)=>{const pa=semanticPriorityForFact(a,section,threads).score,pb=semanticPriorityForFact(b,section,threads).score;return pb-pa||a.e.localeCompare(b.e)||a.p.localeCompare(b.p)||String(a.v).localeCompare(String(b.v));}).slice(0,this.maxFactsPerSection);
    const current=finalize(buckets.current,'current'),historical=finalize(buckets.historical,'historical'),unresolvedRows=finalize(buckets.unresolved,'unresolved');
    const keptIds=new Set([...current,...historical,...unresolvedRows,...threads].map(f=>f.id));for(const id of Object.keys(provenanceIndex))if(!keptIds.has(id))delete provenanceIndex[id];
    const dependenciesList=[...dependencies].sort(),idMaterial=[...current,...historical,...unresolvedRows].map(f=>f.id);if(threads.length)idMaterial.push(...threads.map(t=>t.id));
    const packet=createCompiledContextPacket({id:`packet:${intent.toLowerCase()}:${hash(idMaterial.sort().join('|')||'empty')}`,query,intent,current,historical,unresolved:unresolvedRows,activeThreads:threads,provenanceIndex,dependencies:dependenciesList});
    if(Object.keys(knowledgeTraceIndex).length)packet.knowledgeTraceIndex=knowledgeTraceIndex;
    const metadata={kind:'ContextCompilerMetadata',semanticPriority:buildSemanticPriority({current,historical,unresolved:unresolvedRows,activeThreads:threads}),semanticSizing:computeSemanticSizing({current,historical,unresolved:unresolvedRows,activeThreads:threads,provenanceIndex,dependencies:dependenciesList}),representationEligibility:defaultRepresentationEligibility(),threadDiagnostics:{admittedThreadIds:threads.map(t=>t.threadId),staleThreadIds:threadState.staleThreadIds}};
    return{packet,metadata};
  }
}
