# FT006 Shadow Readiness

Wave 3 does not implement FT006. It exposes bounded metrics later representative-RP qualification can consume: nominated/zero workers, WarmPacket hit/partial/stale outcomes, retrieval HIGH/MIXED/LOW and abstention, Green Room expiry, consolidation backlog pressure, Streaming Truth classifications, fallback/retry/stale-drop counts and result destinations.

Token/cost/CPU/RAM values remain `NOT_MEASURED` unless actual instrumentation supplies them.
