# Coprocessor Wave 14 Acceptance

Status: **CONNECTION/FT005 COPROCESSOR CONTRIBUTION READY FOR CROSS-LANE ASSEMBLY**

This is not closure of #180, #224, or #246. Those require Runtime/live-demo/operator integration beyond this lane.

## Proven locally

Wave 14 covers the optional-resource lifecycle, callable capability profiles, bounded capacity, capability routing, one-resource/multiple-logical-job execution, optional second-resource fallback, structured response validation, malformed-output rejection, unreachable endpoints, timeout, disconnect/abort, Jev deterministic skip, bounded invocation, abstention, sealed/late containment, secret-safe diagnostics, and two unrelated story settings.

The default Brain path remains usable with zero optional resources. Optional resources have no Truth, Precision, settlement, canonical mutation, final-choice, or Context Seal authority.

## Measurement labels

- **LOCAL_DETERMINISTIC**: loopback HTTP transport and deterministic local adapters used by CI.
- **MEASURED_LIVE**: only an operator-configured external/local provider smoke with an actual endpoint/model.
- **SIMULATED**: never reported as measured live.

The CI loopback benchmark measures real host/HTTP execution on the runner but is not evidence of an external provider. Cost is reported only when a usage/pricing receipt exists.

## Required live follow-up

Worker 3 must mount the host actions/read model in the SillyTavern UI. Worker 4 must assemble the host into the live Brain lifecycle and prove admitted provider results join the authoritative Core path without reopening a sealed turn. An operator must run the opt-in smoke with a configured endpoint/model; credentials must remain outside repository evidence.

#224 remains open until the assembled SillyTavern Brain proves the complete operator-visible path.
