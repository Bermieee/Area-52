import {Capability,DeterministicProviderAdapter,NativeSlotKind,createNativeCoprocessorServices,createNativeTurnTask,createOptionalCapabilityTask,evaluateOptionalCapability,executeNativeTurn} from '../src/coprocessor/index.js';
const revisions={sourceRevisionSet:['example:v1'],worldRevision:1,sceneRevision:1,characterStateRevision:1},nativeTask=createNativeTurnTask({...revisions}),nativeOnly=createNativeCoprocessorServices(),nativeResult=executeNativeTurn({task:nativeTask,candidates:[{optionId:'KEEP',evidenceRefs:['E1']}],currentRevisionState:revisions});
const providerId='example-local-jev',withSlots=createNativeCoprocessorServices({optionalSlots:[{kind:NativeSlotKind.JEV,slotId:'slot:jev',profileId:'profile:jev',providerId,local:true,capabilities:[Capability.SEMANTIC_JUDGMENT],adapter:new DeterministicProviderAdapter({providerId,capabilities:[Capability.SEMANTIC_JUDGMENT],handler:()=>({payload:{}})})}]}),optionalTask=createOptionalCapabilityTask({taskId:'example:optional',turnId:'example',correlationId:'corr:example',softDeadline:Date.now()+50,hardDeadline:Date.now()+100,inputRevisionSet:revisions}),eligibility=evaluateOptionalCapability(withSlots,optionalTask,{preferLocal:true});
console.log(JSON.stringify({nativeOnly:{inventory:nativeOnly.inventory,result:nativeResult},withConfiguredSlots:{inventory:withSlots.inventory,eligibility:{status:eligibility.status,reason:eligibility.reason,eligible:eligibility.eligible.map(x=>x.profileId)}}},null,2));